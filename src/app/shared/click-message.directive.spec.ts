import { ClickMessageDirective } from './click-message.directive';

describe('ClickMessageDirective', () => {
  it('should create an instance', () => {
    const directive = new ClickMessageDirective();
    expect(directive).toBeTruthy();
  });
});
